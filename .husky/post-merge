#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
 
yarn run post-merge

CHANGED_FILES="$(git diff-tree ORIG_HEAD HEAD -r --name-only --no-commit-id)"
git diff --exit-code --no-patch ORIG_HEAD HEAD package.json && yarn install